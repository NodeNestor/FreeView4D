# Runtime requirements for FreeView4D's own code.
# Deep deps (torch, gsplat, sam2, hyworld2) are handled by setup/install.sh because
# they need to match WorldMirror's pinned PyTorch 2.4.0 + cu124.

numpy>=1.26,<2.0
Pillow>=10.0
opencv-python>=4.8
scipy>=1.11
plyfile>=1.0
imageio>=2.31
imageio-ffmpeg>=0.4
PyYAML>=6.0
tqdm>=4.65
