--- a/hyworld2/worldrecon/hyworldmirror/models/layers/attention.py
+++ b/hyworld2/worldrecon/hyworldmirror/models/layers/attention.py
@@ -1,3 +1,6 @@
+# Modified by FreeView4D contributors: make flash-attn import truly optional with
+# a fall-through to torch's scaled_dot_product_attention. See FreeView4D/setup/patches/.
+
 # References:
 #   https://github.com/facebookresearch/dino/blob/master/vision_transformer.py
 #   https://github.com/rwightman/pytorch-image-models/tree/master/timm/models/vision_transformer.py
@@ -10,9 +13,15 @@
 try:
     from flash_attn_interface import flash_attn_func as flash_attn_func_v3
     _USE_FLASH_ATTN_V3 = True
+    _HAS_FLASH = True
 except ImportError:
-    from flash_attn.flash_attn_interface import flash_attn_func as flash_attn_func_v2
-    _USE_FLASH_ATTN_V3 = False
+    try:
+        from flash_attn.flash_attn_interface import flash_attn_func as flash_attn_func_v2
+        _USE_FLASH_ATTN_V3 = False
+        _HAS_FLASH = True
+    except ImportError:
+        _USE_FLASH_ATTN_V3 = False
+        _HAS_FLASH = False
 from ...comm.padding import minimal_pad_to_divisible, depad_by_length, pad_by_length
 import torch.distributed as dist
 from ...comm.communication import _All2All, _Allgather
@@ -56,7 +65,7 @@
         return q, k, v, B, N, C

     def _apply_attention(self, q: Tensor, k: Tensor, v: Tensor) -> Tensor:
-        if q.dtype==torch.bfloat16 or q.dtype==torch.float16:
+        if (q.dtype==torch.bfloat16 or q.dtype==torch.float16) and _HAS_FLASH:
             if q.is_contiguous():
                 q = q.transpose(1,2)
             else:
